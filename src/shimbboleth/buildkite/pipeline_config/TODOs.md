- Resitrct the attribute types, and use the existing ones for the field inputs
  e.g. `BoolLikeT` should be the input type, but `bool` should be the attirbute type
  (same for `agents` being a dict)
-
