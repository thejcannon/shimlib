steps:
  - type: command
    plugins:
      - plugin
      - plugin: ~
      - plugin:
          key: value
      # @TODO: Falsey values are ignored? But `0` is invalid? WTF?
      # - plugin: false
      # - plugin: []
      # - plugin: {}

  - type: command
    plugins:
      plugin-null: ~
      # @TODO: Falsey values are ignored? But `0` is invalid? WTF?
      # plugin-false: false
      # plugin-empty-list: []
      # plugin-empty-dict: {}
      pluginobj:
        key: value
---
steps:
  - type: command
    plugins:
      - plugin: ~
      - plugin: ~
      - plugin:
          key: value
  - type: command
    plugins:
      - plugin-null: ~
      - pluginobj:
          key: value
