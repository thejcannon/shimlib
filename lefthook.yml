pre-commit:
  parallel: true
  commands:
    ruff-format:
      tags:
        - formatter
      run: uv run ruff format
    prettier-format:
      tags:
        - formatter
      run: npx prettier --write .
    ruff-check:
      tags:
        - fixer
      run: uv run ruff check --fix
